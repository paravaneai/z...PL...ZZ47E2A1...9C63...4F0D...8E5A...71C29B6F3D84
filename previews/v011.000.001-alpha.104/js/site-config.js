(function (global) {
    "use strict";

    global.ParavaneSiteConfig = Object.freeze({
        theme: Object.freeze({
            // Keep enabled so visitors can select the most readable presentation.
            switchingEnabled: true,
            defaultTheme: "dark"
        })
    });
})(window);
